<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Fruits Show Page</title>
  </head>
  <body>
    <%- include("../partials/navbar.ejs") %>

    <h1><%= fruit.name %></h1>

    <% if (fruit.isReadyToEat){ %>
    <p>This fruit is ready to eat</p>
    <% } else { %>
    <p>This fruit is not ready to eat</p>
    <% } %>

    <a href="/fruits"><button>Go back to All Fruits</button></a>

    <form action="/fruits/<%= fruit._id %>?_method=DELETE" method="POST">
      <button type="submit">Delete this fruit</button>
    </form>

    <a href="/fruits/<%= fruit._id%>/edit">
      <button>Edit the <%= fruit.name%></button>
    </a>
  </body>
</html>
